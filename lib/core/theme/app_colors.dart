import 'package:flutter/material.dart';

class AppColors {
  // Primary palette
  static const primary = Color(0xFF1E6B2D);
  static const secondary = Color(0xFF14972D);

  // Status colors
  static const success = Color(0xFF039855);
  static const error = Color(0xFFEA1111);
  static const warning = Color(0xFFFFCC00);

  // Background
  static const background = Color(0xFFF3F3F3);

  // Text colors (recommended to add)
  static const textPrimary = Color(0xFF1E6B2D);
  static const textSecondary = Color(0xFF6B7280);
  static const caption = Color(0xFF999999);

  // White & black
  static const whiteText = Colors.white;
  static const darkText = Colors.black;
}
