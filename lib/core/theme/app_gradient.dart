import 'package:flutter/material.dart';

class AppGradients {
  static const button = LinearGradient(
    colors: [
      Color(0xFF14972D), // left
      Color(0xFF1E6B2D), // right
    ],
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
  );

}
